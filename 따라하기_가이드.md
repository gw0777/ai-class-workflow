# 🎓 교수-학생 AI 소통 플랫폼 - 비전공자 따라하기 가이드

이 가이드는 프로그래밍을 모르는 분도 쉽게 따라할 수 있도록 작성되었습니다.
각 단계를 천천히 따라하시면 됩니다.

---

## ✅ 체크리스트

프로그램을 실행하기 위해 아래 항목들을 순서대로 체크하세요.

- [ ] 1단계: Node.js 설치
- [ ] 2단계: Anthropic API 키 발급
- [ ] 3단계: Backend 설치 및 설정
- [ ] 4단계: Backend 실행
- [ ] 5단계: Frontend 설치
- [ ] 6단계: Frontend 실행
- [ ] 7단계: 웹사이트 접속
- [ ] 8단계: 계정 만들기
- [ ] 9단계: 메시지 보내기 테스트

---

## 📋 1단계: Node.js 설치 (5분)

### Windows 사용자:

1. **Node.js 다운로드**
   - 웹브라우저에서 https://nodejs.org 접속
   - 초록색 버튼 "LTS (권장)" 클릭
   - 다운로드된 파일 실행

2. **설치 진행**
   - "Next" 계속 클릭
   - "Install" 클릭
   - 설치 완료 후 컴퓨터 재시작

3. **설치 확인**
   - Windows 검색창에 "cmd" 입력
   - 명령 프롬프트 열기
   - 다음 명령어 입력:
   ```
   node --version
   ```
   - `v18.0.0` 같은 버전 번호가 나오면 성공! ✅

### Mac 사용자:

1. **Node.js 다운로드**
   - 웹브라우저에서 https://nodejs.org 접속
   - 초록색 버튼 "LTS (권장)" 클릭
   - 다운로드된 `.pkg` 파일 실행

2. **설치 진행**
   - "계속" 버튼 클릭
   - 설치 완료

3. **설치 확인**
   - 런치패드에서 "터미널" 앱 실행
   - 다음 명령어 입력:
   ```
   node --version
   ```
   - 버전 번호가 나오면 성공! ✅

---

## 🔑 2단계: Anthropic API 키 발급 (10분)

이 프로그램은 AI를 사용하기 때문에 API 키가 필요합니다.

1. **Anthropic 웹사이트 접속**
   - 브라우저에서 https://console.anthropic.com 접속

2. **계정 만들기**
   - "Sign Up" (회원가입) 클릭
   - 이메일, 비밀번호 입력
   - 이메일 인증 완료

3. **API 키 발급**
   - 로그인 후 왼쪽 메뉴에서 "API Keys" 클릭
   - "Create Key" 버튼 클릭
   - 키 이름 입력 (예: "professor-student-app")
   - 생성된 키를 **복사**하여 메모장에 저장 (⚠️ 다시 볼 수 없습니다!)

4. **크레딧 충전** (필요시)
   - 무료 크레딧이 있으면 바로 사용 가능
   - 없으면 "Billing" 메뉴에서 크레딧 구매

**중요**: API 키는 `sk-ant-api03-...`로 시작하는 긴 문자열입니다.

---

## 💻 3단계: Backend 설치 및 설정 (5분)

### Windows 사용자:

1. **명령 프롬프트 열기**
   - Windows 검색창에 "cmd" 입력
   - 명령 프롬프트 실행

2. **프로젝트 폴더로 이동**
   ```
   cd C:\Users\사용자이름\ai-class-workflow\backend
   ```
   (⚠️ 실제 프로젝트 경로로 변경하세요)

3. **의존성 설치**
   ```
   npm install
   ```
   (3-5분 소요, 여러 파일이 다운로드됩니다)

4. **환경 설정 파일 수정**
   - `backend` 폴더를 탐색기에서 열기
   - `.env` 파일을 메모장으로 열기
   - 다음 부분을 찾아서 수정:
   ```
   ANTHROPIC_API_KEY=your-anthropic-api-key-here
   ```
   를
   ```
   ANTHROPIC_API_KEY=sk-ant-api03-여러분의실제키
   ```
   로 변경
   - 저장 (Ctrl + S)

### Mac 사용자:

1. **터미널 열기**
   - 런치패드에서 "터미널" 실행

2. **프로젝트 폴더로 이동**
   ```
   cd ~/ai-class-workflow/backend
   ```

3. **의존성 설치**
   ```
   npm install
   ```

4. **환경 설정 파일 수정**
   ```
   nano .env
   ```
   - `ANTHROPIC_API_KEY` 부분에 발급받은 키 입력
   - `Ctrl + X` → `Y` → `Enter`로 저장

---

## 🚀 4단계: Backend 서버 실행 (1분)

같은 명령 프롬프트/터미널에서:

```
npm start
```

**성공하면 이런 메시지가 나옵니다:**
```
╔══════════════════════════════════════════════════════════════╗
║  교수-학생 AI 소통 플랫폼 서버가 시작되었습니다             ║
╠══════════════════════════════════════════════════════════════╣
║  Port: 3000                                                  ║
║  Environment: development                                    ║
║  API URL: http://localhost:3000/api                          ║
╚══════════════════════════════════════════════════════════════╝
```

✅ **이 창은 닫지 마세요!** (서버가 계속 실행되어야 합니다)

❌ **에러가 나면?**
- API 키가 올바른지 확인
- 포트 3000이 이미 사용 중인지 확인

---

## 🎨 5단계: Frontend 설치 (5분)

**새로운 명령 프롬프트/터미널 창을 하나 더 열어주세요.**

### Windows:
```
cd C:\Users\사용자이름\ai-class-workflow\frontend
npm install
```

### Mac:
```
cd ~/ai-class-workflow/frontend
npm install
```

(3-5분 소요)

---

## 🌐 6단계: Frontend 서버 실행 (1분)

같은 창에서:

```
npm run dev
```

**성공하면 이런 메시지가 나옵니다:**
```
  VITE v5.0.8  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

✅ **이 창도 닫지 마세요!**

---

## 🌟 7단계: 웹사이트 접속

1. **웹브라우저 열기** (Chrome, Edge, Safari 등)

2. **주소창에 입력:**
   ```
   http://localhost:5173
   ```

3. **로그인 화면이 보이면 성공!** 🎉

---

## 👤 8단계: 계정 만들기

### 교수 계정 만들기

1. **"회원가입" 클릭**

2. **정보 입력:**
   - 역할: **교수** 선택
   - 이름: 홍길동
   - 이메일: professor@test.com
   - 비밀번호: test1234
   - 학과: 체육교육과

3. **"회원가입" 클릭**

4. **로그인 페이지에서 로그인**

### 학생 계정 만들기

1. **오른쪽 위 "로그아웃" 클릭**

2. **다시 "회원가입" 클릭**

3. **정보 입력:**
   - 역할: **학생** 선택
   - 이름: 김철수
   - 이메일: student@test.com
   - 비밀번호: test1234
   - 학과: 체육교육과
   - 학번: 20240001

4. **"회원가입" 클릭**

---

## 📧 9단계: 메시지 보내기 테스트

### 교수로 메시지 보내기

1. **교수 계정으로 로그인**
   - 이메일: professor@test.com
   - 비밀번호: test1234

2. **"메시지 작성" 버튼 클릭**

3. **수신자 선택**
   - "개별 학생" 선택
   - 김철수 학생 체크

4. **"다음 단계" 클릭**

5. **메시지 작성**
   ```
   과제를 제출하지 않은 학생들이 있습니다.
   정말 실망스럽네요! 다음부터는 꼭 제출하세요.
   ```

6. **"AI 분석 시작" 클릭**

7. **AI 분석 결과 확인**
   - 감정 분석 결과
   - 위험도 (높음으로 나올 것)
   - 개선된 메시지 확인

8. **"다음 단계" → "메시지 전송"**

### 학생으로 메시지 받기

1. **로그아웃**

2. **학생 계정으로 로그인**
   - 이메일: student@test.com
   - 비밀번호: test1234

3. **받은 메시지 확인**
   - 읽지 않은 메시지 1개
   - 메시지 클릭하여 읽기
   - 피드백 버튼 클릭

---

## ❓ 문제 해결

### "Cannot connect to backend" 에러
- Backend 서버가 실행 중인지 확인
- 포트 3000에서 실행 중인지 확인

### "AI analysis failed" 에러
- `.env` 파일의 API 키 확인
- Anthropic 계정에 크레딧이 있는지 확인

### 화면이 안 나와요
- Frontend 서버가 실행 중인지 확인
- 주소가 정확한지 확인: `http://localhost:5173`

### npm install이 안 돼요
- 인터넷 연결 확인
- Node.js가 제대로 설치되었는지 확인

---

## 📞 도움이 필요하면?

1. **명령 프롬프트/터미널의 에러 메시지를 캡처**
2. **어느 단계에서 멈췄는지 확인**
3. **README.md의 문제 해결 섹션 참조**

---

## ✨ 성공하셨나요?

축하합니다! 🎉

이제 다음을 할 수 있습니다:
- 그룹 만들기
- 여러 학생에게 메시지 보내기
- AI가 메시지를 어떻게 개선하는지 확인

**즐거운 사용 되세요!** 😊
