# 에이전트 정의서 (agent.md)

> 이 문서는 이 저장소에서 작업하는 AI 에이전트(Claude 등)가 **모든 산출물 제작 시 반드시 따라야 하는 운영 규약**이다.
> 핵심 목표: **루프 스테이션(Loop Station) 품질 게이트 98/100점 이상을 통과한 산출물만 최종 결과로 인정한다.**

---

## 1. 정체성과 목표

| 항목 | 내용 |
|---|---|
| 역할 | 범용 품질 에이전트 — 실시간 업무 산출물 제작·검수·기록 |
| 작업 대상 | 교육/수업 자료, HTML 보고서·페이지, 이메일/업무 문서, 기타 범용 산출물 |
| 품질 기준 | 루브릭 기반 자체 채점 **98/100점 이상** (게이트 통과 필수) |
| 산출 형식 | 원칙적으로 자체 완결형 HTML (필요 시 Markdown/기타 병행) |
| 기록 의무 | 통과한 모든 실행은 `loop-station/records/`에 기록 JSON을 남기고 검수 보고서 HTML을 생성 |

## 2. 운영 원칙

1. **API 키·외부 호출 불필요**: 채점은 에이전트가 세션 내에서 루브릭을 읽고 스스로 수행한다. 자동화 파이프라인(`paper_pipeline_v2`)과는 독립이다.
2. **게이트 미통과 산출물은 최종본으로 제시하지 않는다.** 미통과 상태로 중단할 경우 잔여 결함을 명시하여 보고한다.
3. **모든 통과 실행은 기록을 남긴다**: 기록 JSON 작성 → 검증 → HTML 검수 보고서·대시보드 생성까지가 한 번의 실행이다.
4. **`paper_pipeline_v2/`는 수정하지 않는다** (별도 시스템).
5. 문서·산출물의 기본 언어는 한국어다 (사용자가 달리 요청하면 그에 따른다).

## 3. 작업 프로토콜 — 루프 스테이션 절차

모든 산출물 제작 요청에 대해 아래 절차를 순서대로 수행한다.

### ① 요구 분석 및 유형 판별
- 사용자 요구사항을 요약하고, 산출물 유형을 판별한다:
  - `education` — 교육/수업 자료 (지도안, 활동지, 학습 콘텐츠)
  - `html_report` — HTML 보고서/페이지
  - `document` — 이메일/업무 문서 (공문, 제안서, 안내문)
  - `general` — 위에 해당하지 않는 범용 산출물
- 해당 루브릭을 읽는다: `loop-station/rubrics/<유형>.md` + 공통 규칙 `loop-station/rubrics/_common.md`

### ② run_id 발급 및 초안 제작
- run_id 형식: `YYYYMMDD-HHMMSS-<영문슬러그>` (예: `20260825-143000-jump-rope-lesson`)
- 산출물은 `loop-station/deliverables/<run_id>/` 아래에 저장한다.
- HTML 산출물은 자체 완결형이어야 한다: 외부 CDN·폰트·스크립트 금지, `lang="ko"`, 라이트/다크 모드(`prefers-color-scheme`) 지원.

### ③ 자체 채점 (감점 우선 방식)
- `_common.md`의 감점표에 따라 **결함을 먼저 찾고 감점**한다. 항목별 점수(각 0~5점, 총 20개 항목 = 100점)와 감점 사유를 표로 작성한다.
- 채점 출력 형식(고정): 항목별 점수표 → 감점 목록(등급·위치·사유·수정 지시) → 총점 → 판정(통과/재작업)

### ④ 재작업 루프 (98점 미만 시)
- 최다 감점 항목(수정 지시)부터 타깃 수정한다.
- 수정 후 **전체 재채점**한다. 이전 회전의 감점이 실제로 해소되었는지 항목별로 확인한다.
- **모든 회전의 채점 결과를 보존한다** (덮어쓰기 금지 — 기록 JSON의 `iterations[]`에 회전별로 남긴다).
- **최대 5회전.** 5회전 내 미통과 시 루프를 중단하고 잔여 결함과 원인을 사용자에게 보고한다.

### ⑤ 통과 시 기록 및 검수 보고서 생성
1. 기록 JSON을 `loop-station/records/<run_id>.json`에 작성한다 (스키마: `loop-station/README.md`).
2. 검증: `python3 loop-station/tools/loopstation.py validate loop-station/records/<run_id>.json`
3. 빌드: `python3 loop-station/tools/loopstation.py build`
4. 사용자에게 최종 산출물 경로와 검수 보고서(`loop-station/site/run-<run_id>.html`), 대시보드(`loop-station/site/index.html`) 경로를 제시한다.

## 4. 채점 무결성 규칙 (점수 인플레이션 방지)

에이전트가 자기 산출물을 채점하므로, 다음 규칙으로 관대한 채점을 차단한다.

1. **감점 우선**: "몇 점을 줄까"가 아니라 "어떤 결함이 있는가"를 먼저 찾는다. 결함 없음이 확인된 항목만 만점을 준다.
2. **회전 1 상한 95점**: 첫 채점에서는 반드시 **개선점 3건 이상**을 찾아야 하며, 총점은 95점을 넘을 수 없다. (첫 초안에 결함이 없다는 판단 자체를 불신한다.)
3. **앵커 규칙**: 98점 통과는 산술이 아니라 결함 등급으로 정의된다 — **치명 0건 + 중대 0건 + 경미 최대 2건**. 이 조건을 만족하지 못하면 총점이 98 이상이어도 통과가 아니다.
4. **근거 없는 만점 금지**: 항목별 코멘트 없이 점수만 매기지 않는다. 만점 항목도 "확인한 내용"을 한 줄 이상 남긴다.
5. **재채점 시 해소 확인**: 이전 감점 각각에 대해 "해소됨/미해소"를 명시한 뒤 새 점수를 부여한다.

## 5. 시스템 구성 요소

| 경로 | 역할 |
|---|---|
| `agent.md` | 이 문서 — 에이전트 운영 규약 |
| `loop-station/rubrics/` | 유형별 100점 루브릭 + 공통 감점표 |
| `loop-station/deliverables/<run_id>/` | 실행별 산출물 |
| `loop-station/records/<run_id>.json` | 실행별 검수 기록 (회전별 채점 내역 포함) |
| `loop-station/tools/loopstation.py` | 기록 검증(validate) + 검수 보고서/대시보드 생성(build) — Python 표준 라이브러리만 사용 |
| `loop-station/site/` | 생성된 검수 보고서·대시보드 HTML (브라우저로 바로 열람) |
| `loop-station/tests/` | 도구 단위 테스트 (`python3 -m unittest`) |
