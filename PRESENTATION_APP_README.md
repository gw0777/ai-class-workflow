# AI 발표 지원 시스템

발표 자료를 업로드하고 AI 기반으로 발표 스크립트, 시간 배분, 예상 질문 등을 자동으로 생성하는 웹 애플리케이션입니다.

## 주요 기능

### 1. 발표 자료 업로드 및 분석
- **지원 형식**: PDF, PPT, PPTX
- **자동 파싱**: 슬라이드별 텍스트 추출
- **통계 제공**: 슬라이드 수, 단어 수, 평균 단어 등

### 2. 발표 스크립트 자동 생성
- **AI 생성**: Claude API를 사용한 자연스러운 스크립트 생성
- **룰 기반**: API 없이도 기본 스크립트 생성 가능
- **슬라이드별 구성**: 각 슬라이드에 맞는 발표 내용 제공
- **핵심 포인트**: 각 슬라이드의 주요 메시지 요약

### 3. 슬라이드별 시간 배분 가이드
- **자동 시간 계산**: 슬라이드 내용 분량에 따른 시간 할당
- **시각화**: 막대 그래프로 시간 배분 표시
- **시간 요약**: 총 시간, 할당 시간, 여유 시간 확인

### 4. 예상 질문 생성 (Q&A 준비)
- **다양한 질문 유형**:
  - 세부사항 질문
  - 실용적 적용 질문
  - 비판적 질문
  - 확장성 질문
- **답변 제공**: 각 질문에 대한 모범 답변
- **관련 슬라이드**: 질문과 관련된 슬라이드 번호 표시

### 5. 발표 팁 및 체크리스트
- **준비사항**: 발표 전 준비해야 할 사항
- **발표 기술**: 효과적인 발표 방법
- **시간 관리**: 시간 배분 팁
- **실수 방지**: 흔한 실수 피하기
- **체크리스트**: 인터랙티브 체크리스트

### 6. 결과 내보내기
- **Markdown 형식**: 모든 결과를 Markdown 파일로 다운로드
- **인쇄 기능**: 브라우저 인쇄 기능 지원

## 설치 및 실행

### 1. 필수 요구사항

- Python 3.8 이상
- pip (Python 패키지 관리자)

### 2. 의존성 설치

```bash
# 가상환경 생성 (선택사항)
python -m venv venv

# 가상환경 활성화
# Linux/Mac:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt
```

### 3. 환경 변수 설정 (선택사항)

AI 기능을 사용하려면 Claude API 키가 필요합니다.

```bash
# .env 파일 생성
cp .env.example .env

# .env 파일 편집하여 API 키 추가
# ANTHROPIC_API_KEY=your_api_key_here
```

**참고**: API 키 없이도 룰 기반 생성으로 애플리케이션을 사용할 수 있습니다.

### 4. 애플리케이션 실행

```bash
python app.py
```

웹 브라우저에서 `http://localhost:5000` 접속

## 사용 방법

### 1단계: 파일 업로드

1. "파일 선택" 버튼을 클릭하거나 파일을 드래그 앤 드롭
2. PDF 또는 PPTX 파일 선택
3. 파일 정보 자동 표시

### 2단계: 설정

1. **발표 시간**: 총 발표 시간 입력 (기본값: 10분)
2. **발표 스타일**: 전문적, 캐주얼, 학술적 중 선택
3. **예상 질문 개수**: 생성할 질문 개수 (1-10개)

### 3단계: 분석 시작

"🚀 분석 시작" 버튼 클릭

### 4단계: 결과 확인

4개의 탭에서 결과 확인:

1. **📝 발표 스크립트**: 슬라이드별 발표 내용
2. **⏱️ 시간 배분**: 시간 할당 시각화
3. **❓ 예상 질문**: Q&A 준비 자료
4. **💡 발표 팁**: 발표 준비 가이드

### 5단계: 결과 내보내기

- **📥 결과 내보내기**: Markdown 파일로 다운로드
- **🖨️ 인쇄**: 브라우저 인쇄 기능 사용

## 프로젝트 구조

```
ai-class-workflow/
├── app.py                      # Flask 메인 애플리케이션
├── config.py                   # 설정 파일
├── requirements.txt            # Python 의존성
├── .env.example               # 환경 변수 템플릿
├── utils/
│   ├── __init__.py
│   ├── parser.py              # PDF/PPT 파싱
│   └── ai_generator.py        # AI 콘텐츠 생성
├── templates/
│   └── index.html             # 메인 HTML
├── static/
│   ├── css/
│   │   └── style.css          # 스타일시트
│   └── js/
│       └── app.js             # JavaScript 로직
└── uploads/                    # 업로드된 파일 저장
```

## 기술 스택

### Backend
- **Flask**: 웹 프레임워크
- **PyPDF2**: PDF 파싱
- **pdfplumber**: 고급 PDF 텍스트 추출
- **python-pptx**: PowerPoint 파싱
- **anthropic**: Claude API (선택)

### Frontend
- **HTML5**: 구조
- **CSS3**: 스타일링 (그라디언트, 애니메이션)
- **JavaScript**: 인터랙션 및 AJAX

## API 엔드포인트

### `GET /`
메인 페이지 렌더링

### `POST /upload`
파일 업로드 및 파싱

**요청**:
- `file`: 업로드할 파일 (multipart/form-data)

**응답**:
```json
{
  "success": true,
  "filename": "presentation.pdf",
  "summary": {
    "total_slides": 15,
    "total_words": 1500,
    "avg_words_per_slide": 100,
    "file_type": "pdf"
  },
  "parsed_data": {...}
}
```

### `POST /analyze`
전체 분석 수행

**요청**:
```json
{
  "parsed_data": {...},
  "total_time": 10,
  "style": "professional",
  "qa_count": 5
}
```

**응답**:
```json
{
  "success": true,
  "script": {...},
  "qa": {...},
  "tips": {...}
}
```

### `GET /health`
서버 상태 확인

**응답**:
```json
{
  "status": "healthy",
  "ai_enabled": true
}
```

## AI 기능

### AI 활성화 (Claude API)

Claude API를 사용하면 더 자연스럽고 정교한 콘텐츠를 생성할 수 있습니다:

- **발표 스크립트**: 청중의 관심을 끄는 자연스러운 표현
- **예상 질문**: 발표 내용에 기반한 실제적인 질문
- **맞춤형 답변**: 각 질문에 대한 구체적인 답변

### 룰 기반 생성

API 키 없이도 기본적인 기능 제공:

- **기본 스크립트**: 슬라이드 내용 기반 스크립트
- **표준 질문**: 템플릿 기반 질문
- **일반 팁**: 사전 정의된 발표 팁

## 예제 사용 사례

### 1. 학술 발표
- 스타일: Academic
- 시간: 15분
- 질문: 8개

### 2. 비즈니스 프레젠테이션
- 스타일: Professional
- 시간: 10분
- 질문: 5개

### 3. 캐주얼 발표
- 스타일: Casual
- 시간: 5분
- 질문: 3개

## 문제 해결

### 파일 업로드 실패
- 파일 형식 확인 (PDF, PPT, PPTX만 지원)
- 파일 크기 확인 (최대 16MB)

### AI 기능 비활성화
- `.env` 파일에 API 키 설정 확인
- API 키 유효성 확인

### 파싱 오류
- PDF: 스캔본이 아닌 텍스트 기반 PDF 사용
- PPTX: 최신 형식의 PowerPoint 파일 사용

## 라이선스

MIT License

## 기여

이슈와 풀 리퀘스트를 환영합니다!

## 연락처

프로젝트에 대한 문의사항이 있으시면 이슈를 등록해주세요.

---

**AI 기반 체육교육 워크플로우 프로젝트의 일부입니다.**

*Last Updated: 2025-10-21*
