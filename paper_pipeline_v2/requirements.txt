fastapi>=0.115
uvicorn[standard]>=0.30
mcp>=1.2.0
anthropic>=0.40
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3

# DOCX 내보내기(md_to_docx.py)
python-docx>=1.1

# 결제(Stripe 웹훅 서명 검증 — 선택; 미설치 시 개발용 JSON 파싱으로 동작)
stripe>=7.0

# 테스트
pytest>=8.0
